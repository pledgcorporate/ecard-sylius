# dev environment
.env*
!.env.example

# sylius / symfony application files (created by make install)
/vendor/
/node_modules/
/var/
/public/build/
/public/bundles/

# TLS certificates — generated locally via mkcert, used by traefik, never committed
/certs/*.crt
/certs/*.key
/certs/*.pem

# Keep the certs directory itself tracked
!/certs/.gitkeep

# IDE
/.idea/
/.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

